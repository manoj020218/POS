# Packages

Current shared workspace packages:

- `@smart-pos/printer` for receipt-oriented printer contracts, ESC/POS rendering, and transport adapters
- `@smart-pos/client-data` for offline-first client repositories, sync orchestration, and printer-aware checkout
